apiVersion: v1
kind: Service
metadata:
  name: herd-gateway-svc
  labels:
    app: herd
spec:
  ports:
    - name: http
      port: 11434
      targetPort: 11434
  selector:
    app: herd
  type: ClusterIP # Change to NodePort or LoadBalancer if you want to expose it outside the K8s cluster
